// TEMEL — Prettier config (FAZ 3: format entegrasyonu)
// Çakışma önlemi: format ESLint'te YOK, yalnız Prettier ayrı araç (Bölüm 3.4 K1).
// TypeScript built-in destek — ek plugin GEREKMEZ.
export default {
  singleQuote: true,
  semi: true,
  trailingComma: 'all',
  printWidth: 100,
  tabWidth: 2,
  plugins: [],
}
